---
name: alice-absorb
description: 技能包吸收手册（AI 读 md 判类拟描述，脚本落库自检）。触发词：吸收、新技能、加技能、入库、absorb。
x-alice-class: assist
---

# alice-absorb — 吸收新技能进 Alice 模组库（AI 主导判类）

> 角色分工：**AI 判类拟描述，脚本机械落库**。脚本不做关键词猜测；`absorb_skill.py` 拒绝无类目执行（exit 2）是设计行为，不是故障。

## 流程（AI 主导）

1. 拿到新技能目录或 .zip → **AI 先完整读其 SKILL.md**（不许读一半就判）。
2. 按六类定义判类：
   - **破** crack：授权 / 卡密 / 激活 / 注册机 / VIP
   - **逆** reverse：逆向 / 脱壳 / Hook / 取证
   - **渗** pentest：Web / SQL / 端口 / 资产
   - **挂** game：内存 / ESP / 反作弊
   - **智** ai：LLM 越狱 / 提示注入 / MCP / RAG
   - **助** assist：元技能 / 平台 / 工程化
   判不出 → 向用户问一句，不瞎猜。
3. 拟 ≤40 字中文描述（越短越好但必须清楚，含触发词）。
4. 执行落库（目录或 zip 均可）：
   ```bash
   python ".../aliceskill/scripts/absorb_skill.py" <源> --class <crack|reverse|pentest|game|ai|assist> --desc "<中文描述>"
   ```
   退出码含义：`0` 成功；`2` 类目/描述缺失或非中文 → 按输出引导补参重跑（引导里含真实绝对路径的命令行模板）。
5. 重跑生成器：
   ```bash
   python ".../aliceskill/scripts/rebuild_menu.py"
   ```
6. 三自检：
   ```bash
   python ".../aliceskill/scripts/alice_router.py selfcheck"    # 6/6
   python ".../aliceskill/scripts/alice_contract.py selfcheck"  # 5/5
   python ".../aliceskill/scripts/check_auth_policy.py"         # 9/9
   ```
7. 打开 `skills/alice-<类>/SKILL.md` 确认新条目 `- <id>` 与中文描述出现。
8. 报「吸收完成」+ 类目 / 描述 / 索引位置摘要。失败报真实输出并停。

## 进度汇报（硬性）

- 吸收全程向用户实时汇报，格式：`当前进度：N%｜已完成：…｜下一步：…`（按 8 步折算：读 md 13%、判类 25%、拟描述 38%、落库 50%、重建 63%、三自检 75%、索引确认 88%、完成 100%）。
- 每完成一步立即汇报；失败或中断时输出最后真实百分比和具体错误，禁止静默结束。

## 规则

- 判类拿不准就问用户，禁止瞎猜；脚本永不代替 AI 判类。
- `--desc` 给出即覆写 zh_desc 条目（AI 本次判断优先于旧占位）；必须含中文，否则 exit 2。
- 沙箱演练用 `CODEX_HOME=<临时目录>`，产物全落临时库，主库零污染。
