# 覆盖层与图形

**读取条件**：ESP、覆盖层、渲染、图形 API、世界坐标投影与瞄准计算。

本索引包含 5 个主归组模块。只在任务命中本领域时读取，不要为了比较分数读取其他领域索引。

## 选择规则

1. 先按用户目标和当前输入筛除不相关模块。
2. 候选能力接近时优先评分更高者；无评分不等于不可用。
3. 默认先读一个模块，明确存在能力缺口时再增加，每阶段最多 4 个。
4. 没有合适候选时返回 `../SKILL.md`，或使用模型自带知识规划并告知用户步骤、成本和交付路径。

## 模块索引（已评分项按分数降序）

- `graphics-api` 【8/10】 — 图形 API 钩取与覆盖层渲染
- `aimbot-calc` 【4/10】 — 自瞄最优角度计算（FOV/目标预测）
- `overlay-render` 【4/10】 — 透明覆盖层渲染（DirectX）
- `view-matrix` 【4/10】 — 视图投影矩阵定位
- `world-to-screen` 【3/10】 — 3D 世界坐标转屏幕投影

选定后完整读取 `../../_modules/<MODULE_ID>/SKILL.md` 再执行。
