# 综合流程与工具

**读取条件**：游戏研究编排、模拟器、工具准备与综合工作流。

本索引包含 3 个主归组模块。只在任务命中本领域时读取，不要为了比较分数读取其他领域索引。

## 选择规则

1. 先按用户目标和当前输入筛除不相关模块。
2. 候选能力接近时优先评分更高者；无评分不等于不可用。
3. 默认先读一个模块，明确存在能力缺口时再增加，每阶段最多 4 个。
4. 没有合适候选时返回 `../SKILL.md`，或使用模型自带知识规划并告知用户步骤、成本和交付路径。

## 模块索引（已评分项按分数降序）

- `seagull-game-hack` 【8/10】 — 游戏运行时分析：进程内存结构、实体组件、坐标投影可视化、输入模拟与运行时补丁
- `full-game` 【5/10】 — 完整游戏增强链
- `eni-five-edge` 【4/10】 — 寒霜 MAXIMUM CORE 破甲通道。口语 incoming 归一后走逆向、破解、渗透、爬虫、外挂其中一条链并直接交

选定后完整读取 `../../_modules/<MODULE_ID>/SKILL.md` 再执行。
