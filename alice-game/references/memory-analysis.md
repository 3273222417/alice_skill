# 内存与数据定位

**读取条件**：内存读取、扫描、指针链、实体列表、坐标、视矩阵与数值定位。

本索引包含 6 个主归组模块。只在任务命中本领域时读取，不要为了比较分数读取其他领域索引。

## 选择规则

1. 先按用户目标和当前输入筛除不相关模块。
2. 候选能力接近时优先评分更高者；无评分不等于不可用。
3. 默认先读一个模块，明确存在能力缺口时再增加，每阶段最多 4 个。
4. 没有合适候选时返回 `../SKILL.md`，或使用模型自带知识规划并告知用户步骤、成本和交付路径。

## 模块索引（已评分项按分数降序）

- `seagull-memory` 【8/10】 — 跨平台进程内存与取证：dump/堆/指针链/签名/结构分析（Windows/Linux/Android/IL2CPP/Unreal）
- `pubg-progress-recovery` 【6/10】 — PUBG ESP 项目进度恢复
- `entity-list` 【4/10】 — 游戏实体对象枚举
- `pointer-chain` 【4/10】 — 多级指针链定位
- `exact-scan` 【3/10】 — 精确数值内存扫描（金币/血量/弹药）
- `unknown-scan` 【3/10】 — 未知值模糊扫描（增减变化）

选定后完整读取 `../../_modules/<MODULE_ID>/SKILL.md` 再执行。
