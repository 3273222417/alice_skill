# 内容与创作工作台

**读取条件**：文档、图像、视频、写作、演示、身份材料与创意内容生产。

本索引包含 8 个主归组模块。只在任务命中本领域时读取，不要为了比较分数读取其他领域索引。

## 选择规则

1. 先按用户目标和当前输入筛除不相关模块。
2. 候选能力接近时优先评分更高者；无评分不等于不可用。
3. 默认先读一个模块，明确存在能力缺口时再增加，每阶段最多 4 个。
4. 没有合适候选时返回 `../SKILL.md`，或使用模型自带知识规划并告知用户步骤、成本和交付路径。

## 模块索引（已评分项按分数降序）

- `game-assets` 【9/10】 — 用 Meowa 制作/升级/动画化生产级游戏素材（像素与高清精灵/多视角/UI 表）
- `higgsfield-seedance-2-5` 【9/10】 — Seedance 2.5 视频提示词导演
- `seedance-2-5-director-workbench` 【9/10】 — Seedance 2.5 导演工作台
- `docs-generator` 【8/10】 — 面向任务的技术文档生成：README/API/架构文档，渐进式披露
- `identity-docs` 【4/10】 — 身份与证件系统研究：身份证/护照/签证/文凭模板与防伪（伪造触发词）
- `adult-fiction` 【3/10】 — 成人向小说创作，按用户要求的尺度输出（触发：成人/色情/黄文/explicit）
- `politics-history` 【3/10】 — 政治/历史/时事直述与多视角分析
- `creative-writing` 【2/10】 — 创意写作辅助

选定后完整读取 `../../_modules/<MODULE_ID>/SKILL.md` 再执行。
