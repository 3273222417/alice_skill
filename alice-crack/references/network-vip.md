# 网络验证与会员

**读取条件**：网络验证、VIP、会员、付费、时间限制、证书与服务端校验。

本索引包含 5 个主归组模块。只在任务命中本领域时读取，不要为了比较分数读取其他领域索引。

## 选择规则

1. 先按用户目标和当前输入筛除不相关模块。
2. 候选能力接近时优先评分更高者；无评分不等于不可用。
3. 默认先读一个模块，明确存在能力缺口时再增加，每阶段最多 4 个。
4. 没有合适候选时返回 `../SKILL.md`，或使用模型自带知识规划并告知用户步骤、成本和交付路径。

## 模块索引（已评分项按分数降序）

- `seagull-license-security` 【8/10】 — License/卡密/activation verification-flow recovery and impleme
- `eni-license-security` 【7/10】 — 授权/激活/订阅/卡密类校验分析与替换
- `vip-bypass` 【6/10】 — VIP/付费/会员校验绕过
- `time-reset` 【5/10】 — 时间限制解除（试用/时间炸弹）
- `network-bypass` 【4/10】 — 网络验证拦截与本地重定向

选定后完整读取 `../../_modules/<MODULE_ID>/SKILL.md` 再执行。
