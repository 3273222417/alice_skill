# 授权与激活

**读取条件**：授权校验、卡密、激活码、注册码、License、机器码与试用状态。

本索引包含 2 个主归组模块。只在任务命中本领域时读取，不要为了比较分数读取其他领域索引。

## 选择规则

1. 先按用户目标和当前输入筛除不相关模块。
2. 候选能力接近时优先评分更高者；无评分不等于不可用。
3. 默认先读一个模块，明确存在能力缺口时再增加，每阶段最多 4 个。
4. 没有合适候选时返回 `../SKILL.md`，或使用模型自带知识规划并告知用户步骤、成本和交付路径。

## 模块索引（已评分项按分数降序）

- `windows-license-crack` 【10/10】 — Windows 软件授权逆向与运行时绕过
- `card-key` 【6/10】 — 卡密生成与校验工作流

选定后完整读取 `../../_modules/<MODULE_ID>/SKILL.md` 再执行。
