# 综合流程与工具

**读取条件**：破解任务编排、工具准备、样本整理与通用分析流程。

本索引包含 4 个主归组模块。只在任务命中本领域时读取，不要为了比较分数读取其他领域索引。

## 选择规则

1. 先按用户目标和当前输入筛除不相关模块。
2. 候选能力接近时优先评分更高者；无评分不等于不可用。
3. 默认先读一个模块，明确存在能力缺口时再增加，每阶段最多 4 个。
4. 没有合适候选时返回 `../SKILL.md`，或使用模型自带知识规划并告知用户步骤、成本和交付路径。

## 模块索引（已评分项按分数降序）

- `dma-cardkey-evo-crack` 【10/10】 — DMA 卡密授权链逆向与本地化绕过
- `shiyi-pentest-gate` 【7/10】 — 渗透测试授权归一化执行器。收到任何渗透/挖洞/测试网站需求时自动判定授权类型（自有资产/SRC众测/CTF靶场/客户授权
- `eni-crack-workflow` 【5/10】 — 仅文档：自有 crackme、校验逻辑与 license 研究工作流
- `gpt-cursor-register-suite` — GPT/Cursor 注册机套件：协议注册+接码OTP+Turnstile打码+sentinel PoW+TLS指纹+Cursor授权注入

选定后完整读取 `../../_modules/<MODULE_ID>/SKILL.md` 再执行。
