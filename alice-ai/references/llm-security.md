# LLM 安全测试

**读取条件**：LLM 安全评估、测试、幻觉、过度依赖、数据泄露与模型审计。

本索引包含 5 个主归组模块。只在任务命中本领域时读取，不要为了比较分数读取其他领域索引。

## 选择规则

1. 先按用户目标和当前输入筛除不相关模块。
2. 候选能力接近时优先评分更高者；无评分不等于不可用。
3. 默认先读一个模块，明确存在能力缺口时再增加，每阶段最多 4 个。
4. 没有合适候选时返回 `../SKILL.md`，或使用模型自带知识规划并告知用户步骤、成本和交付路径。

## 模块索引（已评分项按分数降序）

- `securityaudit` 【9/10】 — 深度安全审计（证据式/可修复）
- `eni-llm-security` 【8/10】 — 仅文档：LLM/AI 安全测试工作流
- `llm-overreliance-hallucination` 【8/10】 — 过度信任诱导（幻觉破坏动作）
- `llm-security` 【7/10】 — AI 破限技能
- `llm-testing` 【7/10】 — LLM 安全测试提示集（偏见/泄露/对抗）

选定后完整读取 `../../_modules/<MODULE_ID>/SKILL.md` 再执行。
