# AI 攻防工作流

**读取条件**：AI 攻击链、红队套件、Burp/Agent 集成与综合安全流程。

本索引包含 3 个主归组模块。只在任务命中本领域时读取，不要为了比较分数读取其他领域索引。

## 选择规则

1. 先按用户目标和当前输入筛除不相关模块。
2. 候选能力接近时优先评分更高者；无评分不等于不可用。
3. 默认先读一个模块，明确存在能力缺口时再增加，每阶段最多 4 个。
4. 没有合适候选时返回 `../SKILL.md`，或使用模型自带知识规划并告知用户步骤、成本和交付路径。

## 模块索引（已评分项按分数降序）

- `attack-chain` 【9/10】 — CTF/红队实验室多阶段攻击链编排：侦察→立足→提权→横向→证据打包
- `newapi-redteam-audit` 【9/10】 — 对 New API / one-api 系（OpenAI 兼容中转分发站）做红蓝对抗安全审计。覆盖配置面探测、批量注册链
- `llm-offense-kit` 【8/10】 — LLM 攻击性绕过工具箱（载荷本地生成）

选定后完整读取 `../../_modules/<MODULE_ID>/SKILL.md` 再执行。
